# Backend / API
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1
tzdata==2026.3   # zoneinfo (stdlib) ต้องพึ่ง IANA tz database ภายนอกบน Windows (ไม่มี
                 # system tz database ให้อ่านเหมือน Linux/Mac) — ใช้กับ ZoneInfo("Asia/Bangkok")

# Browser control
# W51: อัปเกรดจาก 1.48.0 -> 1.62.0 — Edge ที่ Windows auto-update ให้บนเครื่อง dev ขึ้นไปถึง
# 151.x แล้ว แต่ playwright 1.48.0 (bundled Chromium รุ่นเก่ากว่ามาก) คุยกับ real Edge/Chrome
# ผ่าน channel="msedge"/"chrome" ไม่ได้เลย (TargetClosedError ทุกครั้งที่ launch()/
# launch_persistent_context() ด้วย channel จริง — ยืนยันแล้วว่าไม่ใช่บั๊กโค้ดฝั่งเรา) 1.62.0
# มาพร้อม bundled Chromium สาย 151.x เดียวกับ Edge ที่ติดตั้งอยู่พอดี แก้ปัญหานี้ได้จริง
playwright==1.62.0

# LLM
anthropic==0.116.0
google-generativeai==0.8.3   # provider สำรอง (Gemini) — agent loop function calling
groq==1.5.0   # ทางเลือกไว้ทดสอบ agent loop ตอนยังไม่มี Anthropic key จริง
# W_openai_oauth: SDK ใช้แค่ Responses API client (client.responses.create) ยิงไปที่
# chatgpt.com/backend-api/codex ผ่าน OAuth token ไม่ใช่ api.openai.com + OPENAI_API_KEY
# ปกติ — ดู core/openai_oauth.py หัวไฟล์สำหรับ risk disclosure เต็ม
openai==3.1.0

# RAG / Vector DB
chromadb==1.5.9

# Security 1.4: เข้ารหัส credentials.json ที่เก็บบนดิสก์ (ดู site_learning/storage.py::
# save_credentials/load_credentials) — Fernet (symmetric, authenticated encryption)
cryptography==50.0.0

# Security 1.5: rate limit per-IP บน endpoint ที่เปิด browser จริง/เปลือง resource
# (POST /tasks, POST /api/site-manual/learn — ดู main.py)
slowapi==0.1.10

# Doc ingestion (manual PDF/DOCX/TXT)
pypdf==5.0.1
python-docx==1.1.2
# pdf/xlsx: อ่านไฟล์ XLSX ที่ user แนบเข้ามาโดยตรง (ต่างจาก 2 ตัวบนที่ใช้กับ RAG manual
# ingestion pipeline เดิม — ดู rag/ingestion.py::load_manual_bytes())
openpyxl==3.1.5

# Benchmark target app (benchmark_target/) — server-rendered HTML, session cookies, form
# uploads. jinja2 already pulled in transitively by fastapi; pinned here since we import it
# directly now.
jinja2==3.1.6
pyyaml==6.0.3   # benchmark_target/catalog/ task definitions (generate_catalog.py, validate_catalog.py)
itsdangerous==2.2.0   # starlette SessionMiddleware signing (benchmark_target auth cookie)
python-multipart==0.0.32   # Form()/UploadFile parsing (benchmark_target login form, uploads)

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.28.1   # ให้ fastapi.testclient.TestClient ใช้ (W10[A])
psutil==7.2.2   # W11[A]: หา/ฆ่า chromium process จริงของ BrowserPool สำหรับ chaos test
# MiniWoB++ evaluation (core/miniwob_eval.py, run.py "miniwob"): ใช้แค่ยืมไฟล์ HTML/JS ของ
# task ที่ bundle มากับ package นี้ (miniwob/html/miniwob/*.html) ไม่ได้เรียก Python/Gym API
# ของ package นี้เลยสักฟังก์ชัน (มันผูกกับ Selenium/Gymnasium action space ที่ไม่ตรงกับ
# agent ตัวนี้ — ดู core/miniwob_eval.py หัวไฟล์)
miniwob==1.0
